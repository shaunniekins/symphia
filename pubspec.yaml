name: symphia
description: "Personalize AI Companion"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  animated_splash_screen: ^1.3.0
  audio_wave: ^0.1.5
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_gemini: ^2.0.3
  flutter_markdown: ^0.7.1
  flutter_tts: ^4.0.2
  get: ^4.6.6
  html: ^0.15.4
  permission_handler: ^11.3.1
  speech_to_text: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/
